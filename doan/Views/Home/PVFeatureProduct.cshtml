@model doan.Models.ViewModel.HomeProductVM


<link rel="stylesheet" type="text/css" href="~/Content/myStyle/CustumerHome.css" />

@*hien thij danh sach san pham*@
@if (Model.FeaturedProduct !=null && Model.FeaturedProduct.Any())
 {
    <section class="container-fluid featureProd">
        <h2>Sản phẩm nổi bật</h2>
        <div class="feature-container">
            @foreach (var product in Model.FeaturedProduct)
                {
                    <div class="fproduct-item">
                        <div>
                            <a href="@Url.Action("ProductDetails", "Home", new {id = product.ProductID})">
                                <img src="@product.ProductImage" />
                            </a>
                        </div>
                        <div>
                            <p class="prodPrice">
                                @product.ProductPrice
                            </p>

                            <p class="prodName">
                                @Html.DisplayFor(modelItem => product.ProductName)
                            </p>
                            <p class="prodCate">
                                @Html.DisplayFor(modelItem => product.Category.CategoryName)
                            </p>
                            <p class="prodSale">
                                Đã bán:&nbsp; @Html.DisplayFor(modelItem => product.OrderDetails.Count)
                            </p>
                        </div>
                    </div>
                }
        </div>
    </section>
    <hr />
 }